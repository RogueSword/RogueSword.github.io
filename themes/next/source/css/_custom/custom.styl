// Custom styles.
.post-body p a {
  color: #0593d3;
  border-bottom: none;
  &:hover {
    color: #0477ab;
    text-decoration: underline;
  }
}

// 主页文章添加阴影效果
.post {
  margin-top: 60px;
  margin-bottom: 60px;
  // padding: 25px;
  -webkit-box-shadow: 0 0 10px rgba(202, 203, 203, .7);
  -moz-box-shadow: 0 0 10px rgba(202, 203, 204, .7);
}